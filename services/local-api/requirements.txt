# Core API (Python 3.13 compatible)
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.0

# Database
sqlalchemy==2.0.29
alembic==1.13.1

# Authentication & security
passlib[argon2]==1.7.4
python-jose[cryptography]==3.3.0

# File & utility handling
python-multipart==0.0.9
orjson==3.9.15

# Slide handling (SVS)
openslide-python==1.3.1
openslide-bin==4.0.0.5
Pillow==10.4.0

# Optional performance (recommended for large WSI tile extraction)
pyvips==2.2.2

# Packaging (build only, not needed in production runtime)
# Install separately in dev env:
# pyinstaller
